{#
/**
 * @file
 * Default theme implementation to display documents action links.
 *
 * Available variables:
 * - actions: the list of documents action links.
 */
#}

{% if actions %}
  <div class="dropdown">
    <button class="documents-item__edit-btn dropdown-toggle" data-toggle="dropdown">
      <i class="fi fi-rr-menu-dots"></i>
    </button>

    <ul class="comment-item__edit-menu dropdown-menu dropdown-menu-right">
      {% for action in actions %}
        {% if action %}
          <li class="dropdown-item">{{ action }}</li>
        {% endif %}
      {% endfor %}
    </ul>
  </div>
{% endif %}
